1.we get an isActive tool in navlink for changing icon color when we hover on it
2.react--router-dom gives us more methods like links navlinks,etc
3.we need to import rouiter through main.jsx after creating it
4.Outlet helps to make the exterior(header footer) same but inside it we can do changes. we need to give it a proper layout
5.we need to tell navlink where we are going like links an dfor that use   to="/About"
6.To make a route, make its route in main.jsx 